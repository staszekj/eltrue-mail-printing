import { process } from '../imap-service'


// jest.mock('../../common/', () => {
//   return {
//     PRINTED_EMAILS_ENDPOINT_PATH: 'OK'
//   }
// });

describe.skip("Test", () => {

  it('should be', async () => {
    expect(true).toBe(true)
  });

  it('should be 1', () => {
    expect(true).toBe(false)
  });
});
